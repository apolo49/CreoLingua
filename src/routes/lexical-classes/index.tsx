import { component$ } from "@builder.io/qwik";
import type { DocumentHead } from "@builder.io/qwik-city";

export default component$(() => {

    // const greetMsg = useSignal('')
  
    return (
      <>
      </>
    );
  });
  
  export const head: DocumentHead = {
    title: "Lexical Class",
    meta: [
      {
        name: "description",
        content: "Lexical Classes",
      },
    ],
  };